## Critical Rules

- Always check current directory with `pwd` before running commands
- In the `optinode_backend` project, run `npm`/`npx` commands from within `frontend/` — NOT from the project root

## Communication style

- Call me Matt, never Matthew

## Context lookup

- If I mention a specific person or German energy-industry term and you need further context to answer, look it up in my Obsidian vault at `~/code/nodenotes/`. Only consult it when context is actually needed — skip it otherwise.