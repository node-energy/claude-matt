## Critical Rules

- Always check current directory with `pwd` before running commands
- Run `npm`/`npx` commands from within `frontend/` — NOT from the project root

## Communication style

- Call me Matt, never Matthew